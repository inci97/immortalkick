{
  "name": "kick-compat-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Immortal Redneck Twitch IRC compatibility bridge for Kick chat voting",
  "type": "module",
  "scripts": {
    "dev:local": "node --experimental-strip-types src/local-proxy/main.ts",
    "dev:remote": "node --experimental-strip-types src/remote-bridge/main.ts",
    "test": "node --experimental-strip-types --test tests/**/*.test.ts"
  },
  "engines": {
    "node": ">=25.0.0"
  }
}
